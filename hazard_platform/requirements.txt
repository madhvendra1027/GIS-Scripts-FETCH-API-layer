fastapi>=0.110
uvicorn[standard]>=0.29
pytest>=8.0

# fetch_sediment_type.py (sediment_type_code, EROSION): queries Google
# Earth Engine's hosted SoilGrids mirror since 2026-09-18, after ISRIC
# paused its own REST API. Needs a one-time GEE service-account setup --
# see fetch_sediment_type.py's module docstring.
earthengine-api>=1.7

# One-time static-dataset ingestion only (load_shoreline_change.py,
# load_mangrove_cover.py, spatial_join.py, load_from_csv.py,
# ingest_dataset.py, convert.py) -- not needed by the live pipeline
# (pipeline_runner.py / backend.api) itself.
pandas>=2.0
geopandas>=0.14
python-dotenv>=1.0
